/* Automatically generated by './build/bin/build-entry.js' */

import SungrowExport from '../packages/sungrow-export/index.js'
import SungrowFooter from '../packages/sungrow-footer/index.js'

const components = [
  SungrowExport,
  SungrowFooter
]

const install = function (Vue, opts = {}) {
  components.forEach(component => {
    Vue.component(component.name, component)
  })
}

/* istanbul ignore if */
if (typeof window !== 'undefined' && window.Vue) {
  install(window.Vue)
}

export default {
  version: '0.0.3',
  install,
  SungrowExport,
  SungrowFooter
}
